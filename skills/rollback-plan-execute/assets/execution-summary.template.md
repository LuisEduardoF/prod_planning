Rollback execution for issue #<N> — <issue title>

Applies the rollback plan posted at <plan comment URL>. Generated by
`skills/rollback-plan-execute`; the plan and its cited context documents are the
source of truth, not this description.

## What changed

- `<path>` — <what changed and why> (plan step <n>)

## Plan steps applied

1. <plan step text> (source: <tag carried over from the plan>)

## Needs sign-off

Implemented from plan steps tagged `⚙ inferred` — no documented procedure backs
these, so confirm the behavior is what you want before merging.

- <plan step text> → `<path>` — <what was assumed>

## Not code — do these by hand

These plan steps are part of the rollback but are not changes to this
repository. Merging this pull request does not perform them.

- [ ] <plan step text> (source: <tag>)

## Already satisfied

- <plan step text> — already handled by `<path>:<line>`

## Checklist follow-ups

The checklists under `checklists/` are generated — this pull request does not
edit them. To make a regenerated checklist contain the item below, change the
context document named, then re-run `scripts/checklist-generate.sh`.

- **Item that should exist:** <checklist item wording>
  **Change in:** `context/<business|product>/<doc>` — <concrete replacement wording>

Closes #<N>
